<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Essential Permissions -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- For Google Drive Sync only -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- For Intruder Selfie & OCR -->
    <uses-permission android:name="android.permission.VIBRATE" /> <!-- For Premium Haptic Feedback -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" /> <!-- For Fingerprint/FaceID -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT" android:maxSdkVersion="28" /> <!-- For legacy Fingerprint sensors -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- For Expiry Reminders (Android 13+) -->

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.fingerprint" android:required="false" />

    <!-- Queries for Intent resolution (Crucial for Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:name=".MemPassApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mempass"
        android:hardwareAccelerated="true">

        <!-- Disable default WorkManager initializer for on-demand initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Main Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Mempass"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Autofill Authentication Activity -->
        <activity
            android:name=".AutofillAuthActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />

        <!-- Autofill Save Prompt Activity -->
        <activity
            android:name=".AutofillSaveActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />

        <!-- Autofill Service Registration -->
        <service
            android:name=".MemPassAutofillService"
            android:label="MemPass Autofill"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
            <meta-data
                android:name="android.autofill"
                android:resource="@xml/autofill_service_config" />
        </service>

        <!-- Secure File Provider for Sharing Documents & Camera Capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.mempass.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>
